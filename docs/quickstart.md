# Quick Start

From the project directory, run:

    python main.py

Select one execution mode:

1. Research mode: uses the private local database.
2. Demonstration mode: uses the public anonymized database.

Then select normal or detailed output.
